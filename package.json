{
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "watch": "npm run dev"
  },
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.25.0",
    "glob": "^10.3.10"
  }
}
